import 'package:flutter/material.dart';
//import 'package:url_launcher/url_launcher.dart';
import 'dart:html';
import 'package:flutter/cupertino.dart';
// como baixar     import 'package:url_launcher/url_launcher.dart';
// Use mix in to extend multiple clsses

/*
class NavigationBar extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.black,
      height: 80,
      child: Row(
        children: <Widget>[
          SizedBox(
            height: 80,
            width: 150,
            child: Image.asset('DaoLogoLifeCoin'),
          ),
          SizedBox(
            height: 80,
            width: 150,
            child: Text('LIFECOIN', style: TextStyle(color: Colors.white)),
          ),
          Spacer(flex: 4,), // pq nao pode usar o raised button?????

        ],
      ),
    );
  }
}


class PrimeiraTelaQueAparece extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 800,
      child: Row(
        children: <Widget>[
          SizedBox(
            height: 300,
            width: 750, //use the function do adapt screens
            child: Text('THE BIGGEST PROJECT FOR DONATIONS', style: TextStyle(color: Colors.white)),
          ),
          //botar as colunas dos botoes


          Spacer(flex: 1,),
          SizedBox(
            height: 780,
            width: 750,
            child: Text('DaoLogoLifeCoin'),
          ),
          Spacer(flex: 4,), // pq nao pode usar o raised button?????

        ],
      ),
    );
  }
}


class SegundaTelaQueAparece extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 650,
      width: 1800,
      child: Row
      (
        children: <Widget>[
          SizedBox(
            height: 300,
            width: 500, //use the function do adapt screens
            child: Text('WE ARE REALLY FOCUSED ON DONATIONS', style: TextStyle(color: Colors.white)),
          ),
          Spacer(flex: 3,),
          SizedBox(
            height: 300,
            width: 500,
            child: Text('imagem BNB'),
          ),
          Spacer(flex: 3,),
          SizedBox(
            height: 300,
            width: 500,
            child: Text('WE AIM TO PROMOTE EQUALITY IN THE WORLD'),
          ),
        ],

      ),
    );
  }
}



class SegundaTelaQueAparecePartDeBaixo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 650,
      width: 1800,
      child: Row
        (
        children: <Widget>[
          SizedBox(
            height: 300,
            width: 500, //use the function do adapt screens
            child: Text('STOP POPPY!', style: TextStyle(color: Colors.white)),
          ),
          Spacer(flex: 3,),
          SizedBox(
            height: 300,
            width: 500,
            child: Text('THE MOST POPULAR BLOCKCHAIN IN THE WORLD'),
          ),
          Spacer(flex: 3,),
          SizedBox(
            height: 300,
            width: 500,
            child: Text('IMAGEM LOGO'),
          ),
        ],

      ),
    );
  }
}





class TerceiraTelaQueAparece extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 800,
      child: Column(
        children: <Widget>[
          SizedBox(
            height: 200,
            width: 750, //use the function do adapt screens
            child: Text('LIFECOIN', style: TextStyle(color: Colors.white)),
          ),
          SizedBox(
            height: 200,
            width: 1500, //use the function do adapt screens
            child: Text('LIFECOIN is decentralized, scalable, deflationary, self-sustainable and aims to make the world a more equal place ', style: TextStyle(color: Colors.white)),
          ),
          Spacer(flex: 1,),
          Row(
            children: <Widget>[
            SizedBox(
              height: 200,
              width: 750, //use the function do adapt screens
              child: Text('imagem com as maos para cima'),
            ),
              SizedBox(
                  height: 200,
                  width: 750, //use the function do adapt screens
                  child: Text('imagem com os objetivos'),
              )
            ]
          ),
        ],
      ),
    );
  }
}


class QuartaTelaQueAparece extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 800,
      child: Column(
        children: <Widget>[
        SizedBox(
        height: 200,
        width: 750, //use the function do adapt screens
        child: Text('FEATURES', style: TextStyle(color: Colors.white)),
        ),
          SizedBox(
            height: 200,
            width: 750, //use the function do adapt screens
            child: Text('imagem com as features', style: TextStyle(color: Colors.white)),
          )
        ],
      ),
    );
  }
}


class QuintaTelaQueAparece extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      height: 800,
      child: Column(
        children: <Widget>[
          SizedBox(
            height: 200,
            width: 750, //use the function do adapt screens
            child: Text('HELPED CHARITIES INSTITUTIONS ', style: TextStyle(color: Colors.white)),
          ),
          SizedBox(
            height: 200,
            width: 750, //use the function do adapt screens
            child: Text('imagem com as features', style: TextStyle(color: Colors.white)),
          )
        ],
      ),
    );
  }
}







CODE FIRST VIDEO
  return Container(
      height: 100,
      child: Row(
        mainAxisAlignment: MainAxisAlignment.spaceBetween,
        children: <Widget>[
          SizedBox(
            height: 80,
            width: 150,
            child: Image.asset('imagem simbolo lifecoin'),
          ),
          Row(
            mainAxisSize: MainAxisSize.min,
            children: <Widget>[
              _NavBarItem('Episodes'),
              SizedBox(
                width: 60,
              ),
              _NavBarItem('About'),
            ],
          )
        ],
      ),
    );
  }
}
 */