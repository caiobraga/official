import 'package:url_launcher/url_launcher.dart';

class DaoLinks {

final linkTwitter = 'https://twitter.com/';
final linkInstagram = '';
final linkWhereToBuy = '';
final linkYoutube = '';
final whitepaper = '';
final linkTelegramBrazilPortuguese = '';
final linkTelegramIndia = '';
final linkTelegramArabic = '';
final linkTelegramSpanish = '';
final linkTelegramKorean = '';
final linkTelegramJapanese = '';
final linkTelegramGermany = '';
final linkTelegramIndonesian = '';
final linkTelegramRussian = '';
final linkTelegramFrench = '';
}


void _launchURLLinkInstagram() async {
  const url = 'https://google.com.br';
  if (await canLaunch(url)) {
    await launch(url);
  } else {
    throw 'Could not launch $url';
  }
}

void _launchURLLinkTelegramBrazilPortuguese() async {
  const url = 'https://google.com.br';
  if (await canLaunch(url)) {
    await launch(url);
  } else {
    throw 'Could not launch $url';
  }
}

void _launchURLLinkTelegramIndiar() async {
  const url = 'https://google.com.br';
  if (await canLaunch(url)) {
    await launch(url);
  } else {
    throw 'Could not launch $url';
  }
}

void _launchURLLinkTelegramArabic() async {
  const url = 'https://google.com.br';
  if (await canLaunch(url)) {
    await launch(url);
  } else {
    throw 'Could not launch $url';
  }
}

void _launchURLLinkWhereToBuy() async {
  const url = 'https://google.com.br';
  if (await canLaunch(url)) {
    await launch(url);
  } else {
    throw 'Could not launch $url';
  }
}

void _launchURLLinkYoutube() async {
  const url = 'https://google.com.br';
  if (await canLaunch(url)) {
    await launch(url);
  } else {
    throw 'Could not launch $url';
  }
}

void _launchURLLinkWhitepaper() async {
  const url = 'https://google.com.br';
  if (await canLaunch(url)) {
    await launch(url);
  } else {
    throw 'Could not launch $url';
  }
}

